ext {
    /**
     * isBuildModule 为 true 时可以使每个组件独立运行, false 则可以将所有组件集成到宿主 App 中
     * isBuildModule=true
     * 修改之后需要Sync方可生效
     */
    isContentModule = false

    isLoginModule = false

    isHomeModule = false

}
